import {Link} from "react-router-dom";
import { Sparkles, Heart, Star } from "lucide-react"

export default function Header(){
    return(
        <header className="fixed top-0 left-0 w-full bg-white shadow-md z-50 px-2 sm:px-4 py-5 lg:py-7">
            <nav
                className="text-base lg:text-lg font-semibold flex items-center justify-between mx-1 sm:mx-2 md:mx-4 lg:mx-4">
                <Link to="/" className="flex items-center gap-3">
                    <Sparkles className="h-10 w-10 text-secondary fill-secondary" />
                    <span className="text-3xl text-secondary sm:text-xl md:text-2xl lg:text-5xl font-bold font-serif transition-colors">Beauty Salon</span>
                </Link>
                <div className="flex items-center gap-1 sm:gap-2 md:gap-3 lg:gap-7 font-sans text-foreground">
                    <Link to="/"
                          className="text-[10px] sm:text-xs lg:text-lg transition-colors hover:text-secondary">HOME</Link>
                    <Link to="/"
                          className="text-[10px] sm:text-xs lg:text-lg transition-colors hover:text-secondary">SERVICES</Link>
                    <Link to="/" className="text-[10px] sm:text-xs lg:text-lg transition-colors hover:text-secondary">SIGN IN</Link>
                    <Link
                        className="rounded-3xl py-1 px-2 sm:py-1.5 sm:px-3 lg:py-3 lg:px-9 text-[10px] sm:text-sm lg:text-lg bg-secondary text-primary-foreground hover:bg-button-hover transition-colors whitespace-nowrap"
                        to="/">Sign Up</Link>
                </div>
            </nav>
        </header>
    )
}